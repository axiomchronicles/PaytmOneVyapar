# Vyapaar Commander

The backend lives in [`backend/`](backend/README.md). It owns business state, forecasting,
agent workflows, A2A negotiation, approvals, secure order execution, channels, and outcome events.

The repository started with no application source or configuration; the backend is therefore
self-contained and does not change any pre-existing client contract.
